<Page
    x:Class="VRC_Favourite_Manager.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VRC_Favourite_Manager.Views"
    xmlns:viewModels="using:VRC_Favourite_Manager.ViewModels"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:models="using:VRC_Favourite_Manager.Models"
    xmlns:converters="using:VRC_Favourite_Manager"
    xmlns:generic="using:System.Collections.Generic">

    <Page.Resources>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Horizontal">
            <NavigationView Height="auto"
                            PaneDisplayMode="Left"
                            IsTabStop="False"
                            IsSettingsVisible="False"
                            SelectionChanged="NavigationView_SelectionChanged">
                <NavigationView.MenuItems>
                    <NavigationViewItem Content="All Worlds" Icon="Globe" Tag="AllWorlds" />
                    <NavigationViewItem Content="Folders" MenuItemsSource="{Binding FoldersNavigationViewItems}"/>
                </NavigationView.MenuItems>
                <NavigationView.FooterMenuItems>
                    <NavigationViewItem Content="Settings" Tag="SettingsPage">
                        <NavigationViewItem.Icon>
                            <FontIcon Glyph="&#xE713;" />
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem Content="Account"/>
                </NavigationView.FooterMenuItems>
            </NavigationView>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <!-- DropDownButton for switching between folders -->
                        <Button Content="Reset Worlds" Command="{Binding ResetCommand}" Margin="0,0,0,0"/>
                </Grid>
                <Grid Grid.Row="1">
                <!-- Frame for displaying the content of the selected folder -->
                    <Frame x:Name="ContentFrame" IsNavigationStackEnabled="False" />
                </Grid>
            </Grid>
        </StackPanel>
    </Grid>
</Page>